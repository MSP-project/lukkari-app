import React from 'react-native';

import SpaceMap from '../components/spaceMap.component';

class SpaceView extends React.Component {
  constructor(props) {
    super(props);
  }

  render() {
    return (
      <SpaceMap/>
    );
  }
}

export default SpaceView;
